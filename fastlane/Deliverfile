# fastlane/Deliverfile
# Default configuration for `deliver` (upload_to_app_store).
# Used when running: bundle exec fastlane deliver

app_identifier "__BUNDLE_ID__"
username "__APPLE_ID__"

submit_for_review true
automatic_release false
force true

# Paths
screenshots_path "fastlane/screenshots"
metadata_path "fastlane/metadata"

# Review Notes (provide login credentials if app requires sign-in)
# submission_information({
#   export_compliance_uses_encryption: false,
#   add_id_info_uses_idfa: false
# })

# Languages
# default_language "en-US"
